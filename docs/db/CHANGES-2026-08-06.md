# Executed live in Supabase — 2026-08-06

All of this is already done in project `dkdxefzhttkmjhdbkvqn`. Nothing here needs re-running.

## Modules removed from the database

- **c4:** `c4_prospects` (97 rows archived), `c4_prospect_activities`, views `c4_pipeline` / `c4_pipeline_by_stage`, helper functions, all 7 `sponsorship_tiers` c4 rows, `role_permissions` module `c4_prospects`. `fundraising_dashboard` rebuilt PAC-only (c4 columns return 0 until the fundraising page drops them).
- **Launch day:** `launch_signups` archived + truncated + anon-revoked (final drop after the code PR); `role_permissions` module `launch` removed.
- **Comms/news:** `role_permissions` module `news` removed (Email page permission key). Pages/API removal is Phase 1 of the code work.
- **Call time:** `call_time_queue` view, `call_dispositions` (archived), `log_call_activity`, `snooze_prospect` dropped.
- Everything snapshotted in the locked-down **`archive`** schema, along with the two old backup tables that were cluttering `public`.

## Access control

- Board members loaded: David Donofrio (treasurer + board_member), Keara Dever (board_member), Brian Sharp (board_member), Ari Childrey (comms_lead + board_member) — their Supabase Auth accounts already existed; they only lacked admin_users rows. All five existing board accounts (Dalma, Chrisondra, Eli, Ariel, Ross) also got the board_member role additively; nobody's current access was reduced.
- Ghost rows deactivated (`admin@`, `adam@`, `ari@ohiopride.org` — no auth accounts ever existed for them).
- **`admin_emails` legacy allowlist retired**: it bypassed roles and `is_active`. `is_admin()` and `has_permission()` now read only `admin_users` + roles. Access truth lives in one place.
- 12 active admin users, all with roles. Four board members have never signed in — send them password resets from /admin/users.

## Contacts / donors cleanup (ActBlue donor rollup 2025-01-01 → 2026-08-06 as source of truth)

- Rollup staged at `archive.actblue_donor_rollup_20260806` (163 donors) and reconciled:
  - `employer` / `occupation` columns added to `founding_members` and `contacts`; filled across the board — donors 164/166 employer (was 3), founding members 162/163 (was 0), plus phone/address/city/state/zip gap-fills. Never overwrote existing values.
  - Missing emails resolved by exact-name match (Kirschnick, Zimmerman, Sharpe check/compliance rows now have their ActBlue identities; their placeholder contacts adopted the emails and cleared review flags).
  - Every ActBlue donor now has a canonical contact; **0 unlinked donors / founding members**.
  - Remaining no-email rows (intentional): Christine Cockley + Friends of Christine Cockley — check contributions with no ActBlue identity; flagged in the review queue.
- New **`contacts_directory`** view: the single streamlined people list (contact + giving rollup + founding/volunteer/network/newsletter flags) that the new Contacts module reads.
- Known follow-ups for the code phase: 3 rollup donors have no `donors` contribution rows (Dennison 8/03, Miranda 8/05, Jackson 7/13) and 23 donors' summed contributions don't match rollup totals — the ActBlue sync only ingests founding-refcode contributions today. Fix in Phase 6, then run a contribution-level CSV backfill.

## Endorsements

- Created `endorsement_reviews`, `endorsement_assignments`, `endorsement_activity` with RLS — the admin page already queried them (they never existed, so voting/assignments silently failed). Board voting works against the live schema now; the mobile-first UI + PDF export is Phase 4.

## Contacts review queue status

- 287 contacts flagged `needs_review`: 118 hard-bounce (email_bounce_review), ~160 signup-sheet OCR / low-confidence transcriptions, plus the check donors above. The Phase 3 review-queue UI is the tool for burning this down.
