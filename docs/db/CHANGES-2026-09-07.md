# Executed live in Supabase — 2026-09-07

Applied to project `dkdxefzhttkmjhdbkvqn` as migration
`endorsement_module_refinements`. Nothing here needs re-running; the same
statements are committed as
`supabase/migrations/20260907000000_endorsement_module_refinements.sql`, and
that file's header explains each change.

## What landed

- `public.ohio_counties` (88 rows, anon-readable) and
  `endorsement_office_options.requires_county` (true for 11 offices: every
  judicial office except the Supreme Court and Court of Appeals, plus County
  Commissioner, the county row offices, Prosecuting Attorney, and Sheriff).
- On `endorsement_applications`: `county` (FK to `ohio_counties`),
  `status_changed_at` (not null, default now, trigger
  `trg_endorsement_stamp_status_changed_at`), `photo_path`,
  `submitted_by_kind` (`candidate` / `campaign_staff` / `pac_staff`, default
  `candidate`), `submitted_by_name`, `submitted_by_role`, `submitted_by_email`.
- `status_changed_at` backfilled from `endorsed_at` for endorsed rows,
  `created_at` for submitted rows, and the activity trail / `reviewed_at`
  otherwise. Verified against the 22 live rows: no row reads the 2026-09-04
  bulk-edit date any more.
- Three rows had the county typed into `district`: Cassandra Rice
  (Montgomery), Sarah Henry (Hamilton), Jessica Miranda (Hamilton). Each now
  carries `county` and a null `district`.
- Storage bucket `endorsement-photos` (private, 8 MB, JPEG/PNG/WebP/HEIC)
  with three `storage.objects` policies: anon insert under
  `submissions/<uuid>/` only, `endorsements:read` select,
  `endorsements:write` all.
- `anon can submit applications` rebuilt to also require `photo_path` to be
  null or inside the row's own `submissions/<id>/` folder.
- `public_endorsements` recreated with `county` appended.

## Side effect to know about

The `set_updated_at` touch trigger stamped `updated_at` on every row during
the backfill. That no longer affects anything the pages show: the endorsement
date is `endorsed_at` and days in stage is `status_changed_at`.
